name: Publish Snapshots

run-name: Publish snapshot for ${{ github.event.workflow_run.head_sha || github.sha }}

on:
  workflow_run:
    workflows:
      - Build all
    types:
      - completed
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gate:
    name: Resolve snapshot request
    runs-on: ubuntu-latest
    outputs:
      publish_sha: ${{ steps.resolve.outputs.sha }}
      publish_branch: ${{ steps.resolve.outputs.branch }}
      should_publish: ${{ steps.resolve.outputs.should_publish }}
      publish_mode: ${{ steps.resolve.outputs.publish_mode }}
      validation_bypassed: ${{ steps.resolve.outputs.validation_bypassed }}

    steps:
      - name: Resolve snapshot
        id: resolve
        shell: pwsh
        env:
          CONFIGURED_BRANCH: ${{ vars.SNAPSHOT_RELEASE_BRANCH }}
        run: |
          $ErrorActionPreference = "Stop"
          $eventPayload = Get-Content -Path $env:GITHUB_EVENT_PATH -Raw | ConvertFrom-Json

          function Skip-Publishing {
              param([string]$Reason)

              "should_publish=false" >> $env:GITHUB_OUTPUT
              Add-Content -Path $env:GITHUB_STEP_SUMMARY -Value "Snapshot publish skipped: $Reason"
              exit 0
          }

          if ($env:GITHUB_EVENT_NAME -eq "workflow_run") {
              $workflowRun = $eventPayload.workflow_run
              if ($workflowRun.event -ne "push") {
                  Skip-Publishing "Build all was triggered by '$($workflowRun.event)', not a push."
              }
              if ($workflowRun.conclusion -ne "success") {
                  Skip-Publishing "Build all concluded with '$($workflowRun.conclusion)'."
              }

              $releaseBranch = if ([string]::IsNullOrWhiteSpace($env:CONFIGURED_BRANCH)) {
                  $eventPayload.repository.default_branch
              } else {
                  $env:CONFIGURED_BRANCH.Trim()
              }

              if ($workflowRun.head_branch -ne $releaseBranch) {
                  Skip-Publishing "Build all ran on '$($workflowRun.head_branch)', not release branch '$releaseBranch'."
              }

              $sha = $workflowRun.head_sha
              $branch = $workflowRun.head_branch
              $publishMode = "automatic"
              $validationBypassed = $false
          } else {
              $sha = $env:GITHUB_SHA
              $branch = $env:GITHUB_REF_NAME
              $publishMode = "manual"
              $validationBypassed = $true
          }

          "should_publish=true" >> $env:GITHUB_OUTPUT
          "sha=$sha" >> $env:GITHUB_OUTPUT
          "branch=$branch" >> $env:GITHUB_OUTPUT
          "publish_mode=$publishMode" >> $env:GITHUB_OUTPUT
          "validation_bypassed=$($validationBypassed.ToString().ToLowerInvariant())" >> $env:GITHUB_OUTPUT
          Add-Content -Path $env:GITHUB_STEP_SUMMARY -Value "Publishing snapshot for $sha from '$branch' in $publishMode mode. Build validation bypassed: $validationBypassed."

  build-package:
    name: Build snapshot package
    needs: gate
    if: needs.gate.outputs.should_publish == 'true'
    runs-on: windows-2025-vs2026
    timeout-minutes: 90
    concurrency:
      group: package-snapshots-snapshot
      cancel-in-progress: false

    steps:
      - name: Checkout publish commit
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          ref: ${{ needs.gate.outputs.publish_sha }}
          submodules: recursive
          fetch-depth: 1
          persist-credentials: false

      - name: Build runtime package
        shell: pwsh
        run: ./publish.ps1 -OfficialBuild $true

      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: snapshot-package
          path: publish/**/*.7z
          if-no-files-found: error
          retention-days: 7

  publish-release:
    name: Publish snapshot release
    needs:
      - gate
      - build-package
    if: needs.gate.outputs.should_publish == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      actions: read
      contents: write
    concurrency:
      group: publish-snapshots-snapshot
      cancel-in-progress: false
    env:
      RELEASE_REPOSITORY: ${{ github.event.repository.fork && github.repository || format('{0}/snapshots', github.repository_owner) }}
      RELEASE_TAG: snapshot
      RELEASE_TITLE: Snapshot build
      PUBLISH_MODE: ${{ needs.gate.outputs.publish_mode }}
      PUBLISH_BRANCH: ${{ needs.gate.outputs.publish_branch }}
      PUBLISH_SHA: ${{ needs.gate.outputs.publish_sha }}
      VALIDATION_BYPASSED: ${{ needs.gate.outputs.validation_bypassed }}

    steps:
      - name: Download package artifact
        uses: actions/download-artifact@v4
        with:
          name: snapshot-package
          path: snapshot-package

      - name: Select downloaded archives
        id: package
        shell: pwsh
        run: |
          $archives = @(Get-ChildItem -Path "snapshot-package" -Recurse -File -Filter "*.7z")
          $runtimeArchives = @($archives | Where-Object { $_.Name -notlike "*-Symbols.7z" })
          $symbolsArchives = @($archives | Where-Object { $_.Name -like "*-Symbols.7z" })

          if ($runtimeArchives.Count -ne 1) {
              $archiveList = if ($runtimeArchives.Count -eq 0) { "<none>" } else { ($runtimeArchives | ForEach-Object { $_.FullName }) -join "`n" }
              throw "Expected exactly one runtime .7z archive, found $($runtimeArchives.Count):`n$archiveList"
          }

          if ($symbolsArchives.Count -ne 1) {
              $archiveList = if ($symbolsArchives.Count -eq 0) { "<none>" } else { ($symbolsArchives | ForEach-Object { $_.FullName }) -join "`n" }
              throw "Expected exactly one symbols .7z archive, found $($symbolsArchives.Count):`n$archiveList"
          }

          $runtimeArchive = $runtimeArchives[0]
          $symbolsArchive = $symbolsArchives[0]
          "runtime_archive_path=$($runtimeArchive.FullName)" >> $env:GITHUB_OUTPUT
          "runtime_archive_name=$($runtimeArchive.Name)" >> $env:GITHUB_OUTPUT
          "symbols_archive_path=$($symbolsArchive.FullName)" >> $env:GITHUB_OUTPUT
          "symbols_archive_name=$($symbolsArchive.Name)" >> $env:GITHUB_OUTPUT
          "Selected runtime archive $($runtimeArchive.FullName)"
          "Selected symbols archive $($symbolsArchive.FullName)"

      - name: Publish snapshot release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.event.repository.fork && github.token || secrets.SNAPSHOTS_TOKEN }}
          RUNTIME_ARCHIVE_PATH: ${{ steps.package.outputs.runtime_archive_path }}
          RUNTIME_ARCHIVE_NAME: ${{ steps.package.outputs.runtime_archive_name }}
          SYMBOLS_ARCHIVE_PATH: ${{ steps.package.outputs.symbols_archive_path }}
          SYMBOLS_ARCHIVE_NAME: ${{ steps.package.outputs.symbols_archive_name }}
        run: |
          $ErrorActionPreference = "Stop"
          if (Get-Variable -Name PSNativeCommandUseErrorActionPreference -ErrorAction SilentlyContinue) {
              $PSNativeCommandUseErrorActionPreference = $true
          }

          if ([string]::IsNullOrWhiteSpace($env:GH_TOKEN)) {
              throw "Release token is not configured."
          }

          $notes = @"
          Source repository: $env:GITHUB_REPOSITORY
          Source branch: $env:PUBLISH_BRANCH
          Source commit: $env:PUBLISH_SHA
          Publish mode: $env:PUBLISH_MODE
          Build validation bypassed: $env:VALIDATION_BYPASSED
          Publish workflow run: $env:GITHUB_SERVER_URL/$env:GITHUB_REPOSITORY/actions/runs/$env:GITHUB_RUN_ID
          "@.Trim()

          $releaseExists = $true
          try {
              gh release view $env:RELEASE_TAG --repo $env:RELEASE_REPOSITORY *> $null
          } catch {
              $releaseExists = $false
          }

          if (-not $releaseExists) {
              gh release create $env:RELEASE_TAG `
                  --repo $env:RELEASE_REPOSITORY `
                  --title $env:RELEASE_TITLE `
                  --notes $notes
          } else {
              gh release edit $env:RELEASE_TAG `
                  --repo $env:RELEASE_REPOSITORY `
                  --title $env:RELEASE_TITLE `
                  --notes $notes

              $release = gh release view $env:RELEASE_TAG --repo $env:RELEASE_REPOSITORY --json assets | ConvertFrom-Json
              foreach ($asset in @($release.assets)) {
                  gh release delete-asset $env:RELEASE_TAG $asset.name --repo $env:RELEASE_REPOSITORY --yes
              }
          }

          gh release upload $env:RELEASE_TAG $env:RUNTIME_ARCHIVE_PATH $env:SYMBOLS_ARCHIVE_PATH `
              --repo $env:RELEASE_REPOSITORY

          Add-Content -Path $env:GITHUB_STEP_SUMMARY -Value "Published snapshot assets to $env:RELEASE_REPOSITORY release '$env:RELEASE_TAG' for $env:PUBLISH_SHA: $env:RUNTIME_ARCHIVE_NAME, $env:SYMBOLS_ARCHIVE_NAME."
